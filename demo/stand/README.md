# stand

Minimal Python demo project for the local vmcp MCP stack.

Upstreams (`filesystem`, `architect_c4`, `agent_lsp`) all point here.

- `src/main.py` — tiny module for filesystem / agent-lsp
- `docs/` — C4 TOML for architect-c4

Run: `python -m src.main` (or `python src/main.py`).
