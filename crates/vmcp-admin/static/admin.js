// Shared admin bootstrap — loaded on every page. Page-specific JS lives
// alongside (e.g. sessions.js, skills.js) and is sourced from the
// page-specific template.
console.log("vmcp admin loaded");
